function osversion(os,version){
    console.log(`The OS name is ${os} and Version ${version}.`)
}

let os = prompt("Enter the Os");
let version = prompt("Enter the version");
osversion(os,version);